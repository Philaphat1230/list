void main() {
  // สร้างลิสต์ว่าง
  List<String> days = [];

  // เพิ่มชื่อวันทั้ง 7
  days.addAll(["จันทร์" ,"อังคาร" ,"พุธ" ,"พฤหัสบดี" ,"ศุกร์" ,"เสาร์" ,"อาทิตย์"]);
  // พิมพ์ชื่อวันทั้งหมด
  for (String day in days) {
    print(day);
  }
}
